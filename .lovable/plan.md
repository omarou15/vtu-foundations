## Plan validé + activation toggle IA\n\n### 3 bugs à corriger\n\n**Bug 1 — Miniatures absentes (toggle off)**\n`MessageAttachments` ne réécoute pas réactivement `attachment_blobs`. Si le blob n'est pas encore committé au mount, `useEffect` reste bloqué sur `null`. Le toggle IA n'a rien à voir — c'est juste un timing race.\n→ Convertir `AttachmentThumb` en `useLiveQuery` sur `attachment_blobs.where(\